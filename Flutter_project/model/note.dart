class Note {
  int id = 0;
  String title;
  String category;
  String content;

  Note(this.title, this.category, this.content);

  void setId(int id) {
    this.id = id;
  }

  // void edit(String content)

  // void delete()
}
